<Window x:Class="CipherForge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CipherForge" Height="484" Width="747"
        MinHeight="200" MinWidth="400">

    <Window.Background>
        <SolidColorBrush Color="#FAFAFA"/>
    </Window.Background>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="TextBoxOutput" 
                 Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                 Margin="0,0,0,5" 
                 TextWrapping="Wrap" Text="Ciphertext or Plaintext" 
                 VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                 FontFamily="Consolas"
                 Background="White"
                 BorderBrush="#CCCCCC" 
                 BorderThickness="1"/>

        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,5,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TextBoxPassphrase" 
                     Grid.Column="0"
                     Margin="0,0,5,0" 
                     TextWrapping="Wrap" Text="Passphrase" 
                     VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                     Height="26" 
                     FontFamily="Consolas"
                     Background="White"
                     BorderBrush="#CCCCCC" 
                     BorderThickness="1"
                     VerticalContentAlignment="Center"/>

            <Button x:Name="ButtonGeneratePassphrase"
                    Grid.Column="1"
                    Content="Generate"
                    Width="80" 
                    Height="26"
                    Background="#D9E7FF"
                    Click="ButtonGeneratePassphrase_Click"/>
        </Grid>

        <Button x:Name="ButtonEncrypt" 
                Grid.Row="2" Grid.Column="0"
                Content="Encrypt" 
                Margin="0,5,5,0" 
                Height="35" 
                Background="#CCE0FF"
                Click="ButtonEncrypt_Click"/>

        <Button x:Name="ButtonDecrypt" 
                Grid.Row="2" Grid.Column="1"
                Content="Decrypt" 
                Margin="5,5,0,0" 
                Height="35" 
                Background="#CCE0FF"
                Click="ButtonDecrypt_Click"/>

    </Grid>
</Window>